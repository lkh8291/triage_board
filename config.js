// Edit before deploying. Both dev (github.com) and GHES are supported —
// just swap apiBase and repo. Don't put secrets here; tokens come from PAT modal.
window.CONFIG = {
  apiBase: 'https://api.github.com',          // GHES: 'https://ghe.example.com/api/v3'
  repo:    'lkh8291/triage_board',             // owner/repo
  branch:  'main',
};
